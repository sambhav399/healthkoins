import { Button } from '@healthkoins/design-system';

export default function Header() {
  return (
    <header id="header" className="header">
      <div className="container">
        Header
      </div>
    </header>
  );
}
